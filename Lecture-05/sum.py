def sum_all(*args): # * = รับพารามิตเเตอร์ได้หลายตัวไม่แน่นอน
   return sum(args)

print(sum_all(1, 2, 3, 4, 5))  # Output: 15

def sum_all(*args): # * = รับพารามิตเเตอร์ได้หลายตัวไม่แน่นอน
   return sum(args)

print(sum_all(1, 2, 3, 4, 5, 6, 7))  # Output: 28

def sum_all(*args): # * = รับพารามิตเเตอร์ได้หลายตัวไม่แน่นอน
   return sum(args)

print(sum_all())  # Output: 28